---
description: 'the facilitator reads the committed Navigator critique and commits the transition; the orchestrator is the only writer of status:, never the Driver or Navigator'
---

# Invariant: facilitator_owns_status_commit

the facilitator reads the committed Navigator critique and commits the transition; the orchestrator is the only writer of status:, never the Driver or Navigator
