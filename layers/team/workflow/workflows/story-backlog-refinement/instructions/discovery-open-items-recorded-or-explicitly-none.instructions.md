---
description: 'before sharpening AC, walk the two iteration-tier Discovery lenses against the under-ready Story + its parent Feature and raise/route each gap as an open_item (kind: clarification) — value/intent to the Central Supervisor, peer-owned to the owning hat, the rest as assumption-with-disclosure for the ★ Story Gate (DoR); or explicitly none. There is NO generative-expansion lens at this tier (a deliberate scope-creep guard — a smuggled Feature-sized concern is a defect, not an opportunity). Lens A slice completeness: every AC testable; the parent Feature''s inherited ISO/IEC 25010 NFRs carried onto this slice (not dropped); actors, edge cases and failure paths named; non-goals explicit. Lens C coherence & over-scoping: fits the parent Feature + sibling Stories + the GSM/systemics anchor; a thin slice with no smuggled Feature-tier scope; typed relations minimal and actionable. Capped at one Discovery round + one pre-gate round.'
---

# Invariant: discovery_open_items_recorded_or_explicitly_none

before sharpening AC, walk the two iteration-tier Discovery lenses against the under-ready Story + its parent Feature and raise/route each gap as an open_item (kind: clarification) — value/intent to the Central Supervisor, peer-owned to the owning hat, the rest as assumption-with-disclosure for the ★ Story Gate (DoR); or explicitly none. There is NO generative-expansion lens at this tier (a deliberate scope-creep guard — a smuggled Feature-sized concern is a defect, not an opportunity). Lens A slice completeness: every AC testable; the parent Feature's inherited ISO/IEC 25010 NFRs carried onto this slice (not dropped); actors, edge cases and failure paths named; non-goals explicit. Lens C coherence & over-scoping: fits the parent Feature + sibling Stories + the GSM/systemics anchor; a thin slice with no smuggled Feature-tier scope; typed relations minimal and actionable. Capped at one Discovery round + one pre-gate round.
