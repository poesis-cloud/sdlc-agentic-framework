---
description: 'the facilitator reads the committed intent / runway / priority confirmations and dispatches them as the reviewed input to each challenger — challengers never read the confirmers or each other directly; it authors no Feature content'
---

# Invariant: facilitator_mediates_no_owner_content

the facilitator reads the committed intent / runway / priority confirmations and dispatches them as the reviewed input to each challenger — challengers never read the confirmers or each other directly; it authors no Feature content
