---
description: 'the facilitator stages the committed acceptance / escaped-defect / security / deployability evidence as the ★ Demo Gate packet — the Central Supervisor reads the staged packet, not the demo participants directly; the orchestrator stages, never decides'
---

# Invariant: facilitator_stages_no_decision

the facilitator stages the committed acceptance / escaped-defect / security / deployability evidence as the ★ Demo Gate packet — the Central Supervisor reads the staged packet, not the demo participants directly; the orchestrator stages, never decides
