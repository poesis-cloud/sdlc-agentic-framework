---
description: 'the ceremony refines the ART funnel Feature SET by iterating this per-Feature body once per Feature (runtime replay, latest-wins) — never batching distinct Features into one author turn; the facilitator sequences Features most-foundational/cheapest first so each pass considers the already-refined siblings'
---

# Invariant: multi_feature_loop_framing

the ceremony refines the ART funnel Feature SET by iterating this per-Feature body once per Feature (runtime replay, latest-wins) — never batching distinct Features into one author turn; the facilitator sequences Features most-foundational/cheapest first so each pass considers the already-refined siblings
