---
description: 'the facilitator opens, sequences each participant turn (never collapsing the ceremony into a single dispatch) and mediates every handoff — subagents are stateless and never communicate with each other; each reads committed artifacts and returns, and the orchestrator routes outputs between turns. It renders only; it authors no Feature content, acceptance criteria, WSJF or structurant rationale'
---

# Invariant: facilitator_authors_no_owner_content

the facilitator opens, sequences each participant turn (never collapsing the ceremony into a single dispatch) and mediates every handoff — subagents are stateless and never communicate with each other; each reads committed artifacts and returns, and the orchestrator routes outputs between turns. It renders only; it authors no Feature content, acceptance criteria, WSJF or structurant rationale
