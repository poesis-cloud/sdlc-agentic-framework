---
description: 'the facilitator reads the committed drafts and dispatches them as the single reviewed input to each challenger — challengers never read the authors or each other directly; it authors no Feature content'
---

# Invariant: facilitator_mediates_no_owner_content

the facilitator reads the committed drafts and dispatches them as the single reviewed input to each challenger — challengers never read the authors or each other directly; it authors no Feature content
