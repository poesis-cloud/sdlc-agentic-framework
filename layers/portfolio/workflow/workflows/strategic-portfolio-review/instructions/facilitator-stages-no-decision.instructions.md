---
description: 'the facilitator stages the re-ranked backlog + folded findings as the decision packet for the Central Supervisor — the human reads the staged packet, not the participants directly; the orchestrator stages, never decides'
---

# Invariant: facilitator_stages_no_decision

the facilitator stages the re-ranked backlog + folded findings as the decision packet for the Central Supervisor — the human reads the staged packet, not the participants directly; the orchestrator stages, never decides
